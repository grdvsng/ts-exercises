export const debug = () => {
}

export const factor = (f: number) => {
}

export const debugProperty = () => {
}

export const format = (fmt: string) => {
}

export const stringable = <T extends { new(...args: any[]): {} }>(constructor: T) => {
}

export const hasmeta = (props: Record<string, unknown>) => {
}